import { environment } from './environments/environment';

export const API_ROUTES = {
  BASE_URL: environment.apiBaseUrl,
  DATA: '/data',
  TEST_ORACLE: '/testOracle',
  FAVE_COLOR_INPUT: '/FaveColorInput',
};
